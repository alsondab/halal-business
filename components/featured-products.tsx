"use client"
import Link from "next/link"
import { products, formatPrice } from "@/lib/data/catalog"
import { useCart } from "@/contexts/cart-context"
export function FeaturedProducts(){const {addItem}=useCart(); return <section className="bg-secondary px-6 py-20 md:px-10"><div className="mx-auto max-w-7xl"><div className="mb-10 flex items-end justify-between"><div><p className="font-mono text-xs uppercase tracking-[.3em] text-primary">La sélection HB</p><h2 className="mt-3 text-4xl">Les essentiels</h2></div><Link href="/products" className="text-sm underline underline-offset-4">Voir le catalogue</Link></div><div className="grid gap-6 sm:grid-cols-2 lg:grid-cols-4">{products.filter(p=>p.featured).map(p=><article key={p.id} className="group"><Link href={`/product/${p.slug}`}><div className="aspect-[4/5] overflow-hidden bg-card"><img src={p.image} alt={p.name} className="h-full w-full object-cover transition duration-500 group-hover:scale-105"/></div><div className="flex items-start justify-between gap-3 pt-4"><div><h3 className="font-sans text-sm font-semibold">{p.name}</h3><p className="mt-1 font-mono text-xs text-muted-foreground">{formatPrice(p.price)}</p></div><button aria-label={`Ajouter ${p.name}`} onClick={(e)=>{e.preventDefault();addItem({id:p.id,name:p.name,price:p.price,image:p.image,handle:p.slug})}} className="border border-primary px-3 py-1 text-xs transition hover:bg-primary hover:text-primary-foreground">+</button></div></Link></article>)}</div></div></section>}
