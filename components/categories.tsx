import Link from "next/link"
import { categories } from "@/lib/data/catalog"
export function Categories(){return <section id="categories-section" className="mx-auto max-w-7xl px-6 py-20 md:px-10"><div className="mb-10 flex items-end justify-between gap-4"><div><p className="font-mono text-xs uppercase tracking-[.3em] text-primary">Explorer</p><h2 className="mt-3 text-4xl">Par intention</h2></div><Link href="/products" className="text-sm underline underline-offset-4">Tout voir</Link></div><div className="grid gap-px bg-border md:grid-cols-3">{categories.map((category)=><Link key={category.id} href={`/products?category=${category.slug}`} className="bg-card p-8 transition-colors hover:bg-primary hover:text-primary-foreground"><span className="font-mono text-xs text-muted-foreground">0{categories.indexOf(category)+1}</span><h3 className="mt-12 text-2xl">{category.name}</h3><p className="mt-3 text-sm leading-6 text-muted-foreground">{category.description}</p></Link>)}</div></section>}
